### Example environment file for the server
# Create a file named .env in this folder and add your real MongoDB connection URL.
# Do NOT commit your real .env file into the repo. The repository's .gitignore already excludes .env

MONGO_URL=mongodb+srv://your-username:your-password@cluster0.example.mongodb.net/YourDBName?retryWrites=true&w=majority

# NOTE: Do NOT paste your real connection string below in this file. Create a local file named `.env` next to this example
# and add a single line like:
# MONGO_URL="mongodb+srv://<user>:<password>@cluster0.oos2scd.mongodb.net/<DBNAME>?retryWrites=true&w=majority&appName=Cluster0"
